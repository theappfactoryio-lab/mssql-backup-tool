<div id="files-section-data">
  <%- include('file-table', { files, csrfToken, oob: false }) %>
</div>
<span id="file-count" hx-swap-oob="outerHTML"><%= t('files.sharedVolumeCount', { count: formatNumber(files.length) }) %></span>
